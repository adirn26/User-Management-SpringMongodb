keycloak:
  realm: springtest
  auth-server-url: http://localhost:8180/auth
  resource: springboot-keycloak
  public-client: true
  bearer-only: true

spring:
  cache:
    type: redis
    redis:
      time-to-live: 60000
      cache-null-values: true